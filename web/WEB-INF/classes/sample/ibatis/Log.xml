<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap      
    PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"      
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="Log">
	<typeAlias alias="Log" type="sample.domain.Log" />

	<delete id="delete" parameterClass="int">
		delete from log where id = #id#
	</delete>

	<insert id="add" parameterClass="Log">
		insert into log (id, logTime, clientIP, portNum, clientID, serverIP, remoteDir, FTPmode, result) values(#id#, #logTime#, #clientIP#, #portNum#, #clientID#, #serverIP#, #remoteDir#, #FTPmode#, #result#)
 	</insert>
 	
	<update id="update" parameterClass="Log">
		update log set logTime = #logTime#, clientIP = #clientIP#, portNum = #portNum#, clientID = #clientID#, serverIP = #serverIP#, remoteDir = #remoteDir#, FTPmode = #FTPmode#, result = #result# where id = #id#
	</update>
 	
	<select id="get" parameterClass="int" resultClass="Log">
		select * from log where id = #id#
 	</select>

	<select id="list" resultClass="Log">
		select * from log order by id
 	</select>
</sqlMap>
